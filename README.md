# strokeseg2-app

## Building

Builds are tested on Windows 11 using MSVC.

This project requires the installation of the following dependencies (all other dependencies are
fetched and built automatically):
- Qt 6.10.0